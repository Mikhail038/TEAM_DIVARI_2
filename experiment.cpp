//==============================================================================================================================================================================

#include "radioactivity.h"

//==============================================================================================================================================================================

#include <stdio.h>
#include <math.h>
#include <stdlib.h>

//==============================================================================================================================================================================

#define MAX_SIZE  100

//==============================================================================================================================================================================

const double beta = 3;

//==============================================================================================================================================================================

int main ()
{
    double start_time = 0,
           end_time   = 0;
    double step = 0;

    scanf ("%lg%lg%lg", &start_time, &end_time, &step);

    double radioactivity[MAX_SIZE] = {};
    double time[MAX_SIZE]          = {};

    int N = experiment (radioactivity, time, start_time, end_time, step);

    return 0;
}

//==============================================================================================================================================================================

int experiment (double* radioactivity, double* time, double start_time, double end_time, double step)
{
    int counter = 0;

    for (double cur_time = start_time; cur_time < end_time; cur_time += step)
    {
        time[counter] = cur_time;

        radioactivity[counter] = exp (- cur_time / beta );
    }

    return counter;
}

void add_noise (double *radioactivity, int N)
{

    return;
}

//==============================================================================================================================================================================
